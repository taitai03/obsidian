フォルダー内検索（グローバル検索）の手順

1. **検索パネルを開く:**
    - キーボードショートカット: **`Ctrl + Shift + F`** (Windows/Linux) または **`Command + Shift + F`** (Mac) を押します。
    - または、サイドバーの虫眼鏡アイコンをクリックします。

```typescript
  const theme = (): RaThemeOptions => {
    const selectEnvTheme = (() => {
      switch (ENVIRONMENT) {
        case 'production':
          return productionTheme
        case 'staging':
          return stagingTheme
        default:
          return developmentTheme
      }
    })()

    return {
      ...selectEnvTheme,
      components: {
        ...(selectEnvTheme.components || {}),
        RaSaveButton: {
          defaultProps: {
            type: 'button',
          },
        },
      },
    }
  }
```
この部分を公式ドキュメントの方式のdeepmergeを使うように変更していく

```typescript
  const selectEnvTheme = (() => {
    switch (ENVIRONMENT) {
      case 'production':
        return productionTheme
      case 'staging':
        return stagingTheme
      default:
        return developmentTheme
    }
  })()

  const theme: RaThemeOptions = deepmerge(selectEnvTheme, {
    components: {
      ...(selectEnvTheme.components || {}),
      RaSaveButton: {
        defaultProps: {
          type: 'button',
        },
      },
    },
  })
```
この時
- before の theme は関数だった → 呼ぶ必要がある
    
- after の theme は値になった → そのまま渡す必要がある
よって
<span style="background:#fff88f">ADminのthemeには関数を渡さない</span>

heheheme={theme} の「意味」を抽象化するとこう > React Admin（または MUI）に> “この UI プロジェクトの設計ルールはこれだよ”> と渡す入口

# React Admin Theme
```typescript
import { Admin, defaultTheme } from 'react-admin';
import { deepmerge } from '@mui/utils';
import indigo from '@mui/material/colors/indigo';
import pink from '@mui/material/colors/pink';
import red from '@mui/material/colors/red';

const myTheme = deepmerge(defaultTheme, {
    palette: {
        primary: indigo,
        secondary: pink,
        error: red,
        contrastThreshold: 3,
        tonalOffset: 0.2,
    },
    typography: {
        // Use the system font instead of the default Roboto font.
        fontFamily: ['-apple-system', 'BlinkMacSystemFont', '"Segoe UI"', 'Arial', 'sans-serif'].join(','),
    },
});

const App = () => (
    <Admin theme={myTheme}>
        // ...
    </Admin>
);
```

（SSR）とは、==Webブラウザからのリクエストに応じて、**サーバー側で完全にレンダリングされたHTMLを生成し、クライアントに送信する**技術==

##  **結論：「まずバックエンド → 次にフロント」がベター。でも状況次第で柔軟に**
今回のように “データ構造と role (enum) の削除／移行” を含む変更なら、**バックエンドを先に直す**のが安全で合理的。

その上で、**フロントを追従 (role の選択肢削除、UI 修正など)**。



