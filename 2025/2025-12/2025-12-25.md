# vscodeでcommand + Z で戻りすぎたとき
shit + cmd +z

## inspectとは
inspectメソッドとは、オブジェクトや配列などをわかりやすく文字列で返してくれるメソッドです。
実はデバッグでよく利用されるpメソッド(標準出力するメソッド)は、このinspectメソッドが使われています。
そのinspectメソッドの構文は以下のように表せます。

## 文字列をエスケープするとは
「文字列として表示させたいもの」を「HTMLとして誤解されない形」に変換すること

##  module ERB::Util
eRubyスクリプトのためのユーティリティーを提供するモジュール
<span style="background:#fff88f">eRubyユーティリティ</span>とは、Rubyの標準ライブラリである`erb`ライブラリに含まれる`ERB::Util`**モジュール**が提供する、eRubyスクリプトを扱うための補助機能群を指します。

## joinメソッド
配列を連結して一つの文字にするメソッド


コードの部分を赤く表示する
cmd + E