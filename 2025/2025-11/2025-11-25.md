やること
- ローカルの環境で、APIキーを変更して動作の確認

## 条件付きレンダー
javascriptのif文や&&,?:演算子など、の構文を使うことで、JSXを条件付きでレンダーできる

**OGPタグの例**


- <meta property="og:title" content="ページのタイトル">
- **<meta property="og:type" content="ページの種類">**
- **<meta property="og:url" content="ページのURL">**
- **<meta property="og:image" content="画像のURL">**
- **<meta property="og:site_name" content="ホームぺージのタイトル">**
- **<meta property="og:description" content="ページの説明">**
- **<meta property="og:locale" content="言語">

### MUIテーマ：reactのライブラリ


# 業務からの学び

HTMLの仕様：フォームの中に,type='submit'
のボタンが存在すると、フォーム内でEnterを押すと==ブラウザが自動==でそのsubmitボタンを実行してフォームを送信する 

read-adminのsavebuttonのデフォルト値はtype="submit"


## 条件（三項）演算子
```javascript
condition ? exprIfTrue : exprIfFalse
```
condition:値が条件として使用される式
exprIfTrue:conditionが真値と評価される場合に表示されるしき
exorIfFalse:conditionが偽値と評価される場合に表示されるしき


## mapメソッドを使う
```javascript
const people = [
  'Creola Katherine Johnson: mathematician',
  'Mario José Molina-Pasquel Henríquez: chemist',
  'Mohammad Abdus Salam: physicist',
  'Percy Lavon Julian: chemist',
  'Subrahmanyan Chandrasekhar: astrophysicist'
];

export default function List() {
  const listItems = people.map(person =>
    <li>{person}</li>
  );
  return <ul>{listItems}</ul>;
}
```

```javascript
const <自分で決めた名前> =[配列名].map(<自分で決めた単数変数>=>
	<li>{<自分で決めた単数変数>}</li>
)
```
filterは条件を絞り込む

```javascript
const chemists = people.filter(person =>  
person.profession === 'chemist'  
);
```

## 純粋関数とは
- 自分の仕事に出力
- 同じ入力には同じ出力

