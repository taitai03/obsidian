```ruby
      jst_today = Time.current.into_time_zone('Asia/Tokyo')

      entries = Entry
                .eager_load(:user)
                .not_declined_status
                .scheduled_joining_on(jst_today.to_date)
```
timezoneをUTCから変更しないと、思わぬ不具合を引き起こす可能性がある

# pluck
- RailsのActiveRecordのメソッドで、データベースから特定のから宇野値を直接取得することができる
- データベースレベルでカラムを指定するため、必要なデータのみを効率的に取得でき、大量のデータを扱う場合にメモリ使用量を節約することができる


rspecをターミナルで
```
make test-file FILE_NAME=spec/tasks/crons/entry_spec.rb
```

:行数でそこだけのテストできる


heroku run rake crons:admin_system_account:notify_recruiter_on_joining_date  -a plexjob-api-feature-mak-mrxkbl

-a 以下は、settingnに行き、revale config varsをして、HEROKU_API_KEYの中身を見る

heroku　ログコマンド
`heroku logs --tail --app`

fat model
https://speakerdeck.com/toshimaru/how-to-deal-with-fat-model?slide=37