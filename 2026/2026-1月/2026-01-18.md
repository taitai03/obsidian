# public privateの境界線について

<span style="background:#fff88f">結論：外から読んでいい責務かどうか</span>
```ruby
class Order
  def calculate_total
    subtotal = sum_items
    subtotal + tax(subtotal) - discount(subtotal)
  end

  private

  def sum_items
  end

  def tax(amount)
  end

  def discount(amount)
  end
end
```

public=外部から呼び出す
private=culculate_totalの内部手順


エイリアスメソッド：既存のメソッドに別名をつけること

コールバック関数：あとで読んでもらうために、先に渡しておく関数