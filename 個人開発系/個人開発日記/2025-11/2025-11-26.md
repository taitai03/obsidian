# 作成しているアプリ名：Kings

[めちゃくちゃ参考にしたページ](https://zenn.dev/tomona/scraps/7058061cc3711a)


 React NativeはAndroidとiOSの知識がbuildする時に必要になってくるようなので、それをラップしてくれているexpoを使用する
```
npx create-expo-app@latest <プロジェクト名> --template blank-typescript
```
以下がプロジェクトの始め方
```
To run your project, navigate to the directory and run one of the following npm commands.

- cd Kings
- npm run android
- npm run ios
- npm run web
```

## 開発サーバの起動
```
npx expo start
```

Expo Goをダウンロードして上記コマンドを打って出てきたQRコードをスキャン

## データベースのはSQliteを使う
なぜSQLiteなのか
- 設定不要
- 型がない（強みであり、弱みである）
- バックアップがファイルで可能

## 必要なpackageのインストール
```
> npm install nativewind tailwindcss react-native-reanimated react-native-safe-area-context
```

各モジュールの役割としては

- tailwind CSS：CSS Framework
- NativeWind：React Nativeでtailwindを使えるようにする
- react-native-reanimated：javascript側ではなくNative threadでアニメーションを実行することで滑らかなアニメーションを提供する
- react-native-safe-area-context：デバイスの「安全な表示領域」（デバイスのノッチ、ステータスバー、ホームインジケーター、物理ボタンなどにより使えない領域を考慮したスペース）を認識し、コンテンツの配置を適切に調整する。ただこれはいらないかもしれない。[こちらの記事](https://zenn.dev/onigiri_w2/articles/9b9890c5bb5e85)ではReact Nativeの標準のものでOKと言及している

### Tailwind cssの設定ファイルの記述
```
npx tailwindcss init
```

```Typescript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./App.{js,jsx,ts,tsx}",
    "./app/**/*.{js,jsx,ts,tsx}",
    // 他のカスタムディレクトリがあればここに追加
  ],
  presets: [require("nativewind/preset")],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

### ExpoでNativeWindを動作させるための[[知らない単語|babel]]の設定ファイルを作成して記述
```
touch babel.config.js
```
ファイルの中身
```javascript
module.exports = function (api) {
  api.cache(true);
  return {
    presets: [
      ["babel-preset-expo", { jsxImportSource: "nativewind" }],
      "nativewind/babel",
    ],
    plugins: ['react-native-reanimated/plugin'],
  };
};
```

## Metroの設定

```
npx expo customize metro.config.js
```

ファイルの中身
```javascript
const { getDefaultConfig } = require("expo/metro-config");
const { withNativeWind } = require('nativewind/metro');

const config = getDefaultConfig(__dirname, { isCSSEnabled: true })

module.exports = withNativeWind(config, { input: './global.css' })
```
 metroとは：別名Metroバンドラー（[[知らない単語|バンドル]]について）
 - コードの変換
 - 依存関係の解決
 - アセットの管理
 - Hot Roading
 この四つの役割を持つ
### cssファイルの作成とimport

```
touch ./global.css
```

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```
 
 index.tsにて以下を追加する
 ```typescript
 import "./global.css"
 ```

### app.jsonの編集

```
{
  "expo": {
    "web": {
      "bundler": "metro"
    }
  }
}
```
この部分に"bundler": "metro"これを追加する

## ここにきてnpx expo startしたら以下のエラーが発生

```
Logs for your project will appear below. Press Ctrl+C to exit.
iOS Bundling failed 138ms index.ts (1 module)
 ERROR  index.ts: Cannot find module 'babel-preset-expo'
```

調査の結果、package.jsonに'babel-preset-expo'これがないことが判明。よって以下のコマンドを実行して対処
```
npm install babel-preset-expo
```


## SQLiteのインストール
```
npx expo install expo-sqlite
```

```
 npx expo install expo-file-system expo-asset
```

この二つを実行

```javascript
defaultConfig.resolver.assetExts.push("db");
```
これをmetro.config.jsに追記

新たにhelperフォルダーを作成し、その中にsqlite.tsxファイルを作成する

### SQLiteのサンプルコード

```SQLite
import * as SQLite from "expo-sqlite";

/**
 * SQLiteと接続
 */
const db = SQLite.openDatabase("db");
createTable(); // 「sample_table」というテーブル作成
insert(1, "Taro"); // 「id=1, name=Taro」のデータを登録
insert(2, "Jiro"); // 「id=2, name=Jiro」のデータを登録
select(); // データを取得してログに表示

/**
 * テーブルを作成する
 */
function createTable() {
  db.transaction((tx) => {
    tx.executeSql(
      // 実行したいSQL文
      `create table if not exists sample_table (id integer primary key not null, name text);`,
      // SQL文の引数
      // 必要ないときは空のまま
      [],
      // 成功時のコールバック関数
      () => {
        console.log("create table success");
      },
      () => {
        // 失敗時のコールバック関数
        console.log("create table faile");
        return false;
      }
    );
  });
}

/**
 * データを登録する
 */
function insert(id, name) {
  db.transaction((tx) => {
    tx.executeSql(
      // 実行したいSQL文
      // ?のところに引数で設定した値が順番に入る
      `insert into sample_table values (?, ?);`,
      // SQL文の引数
      [id, name],
      // 成功時のコールバック関数
      () => {
        console.log("insert success");
      },
      () => {
        // 失敗時のコールバック関数
        console.log("insert faile");
        return false;
      }
    );
  });
}

/**
 * データを取得する
 */
function select() {
  db.transaction((tx) => {
    tx.executeSql(
      // 実行したいSQL文
      `select * from sample_table;`,
      // SQL文の引数
      [],
      // 成功時のコールバック関数
      (_, { rows }) => {
        console.log("select success");
        console.log("select result:" + JSON.stringify(rows._array));
      },
      () => {
        // 失敗時のコールバック関数
        console.log("select faile");
        return false;
      }
    );
  });
}
```

この中の、createテーブルを使う.
使用するときは、関数の前にexport を書く


### <span style="background:#b1ffff">ここで一度、画面遷移の機能を実装する</span>
# ~~1. ライブラリのインストール
```
npm install @react-navigation/native @react-navigation/stack
```
# 2.scrennsフォルダを作成
# 3.HomeScreens.tsxとDetailsScreen.tsxファイルを作成
ホーム画面~~
```typescript
function HomeScreen({ navigation }) {
  return (
    <View>
      <Text>Home Screen</Text>
      <Button
        title="Go to Details"
        onPress={() => {
          navigation.navigate('Details');
        }}
      />
    </View>
  );
}
```

~~詳細画面~~
```typescript
function DetailsScreen({ navigation }) {
  return (
    <View>
      <Text>Details Screen</Text>
      <Button
        title="Go back to Home"
        onPress={() => {
          navigation.goBack();
        }}
      />
    </View>
  );
}
```
これだとうまくいかなかった


## 思った通りにいかない時
＝＝＝＞<span style="background:#fff88f"><font color="#ff0000">保存しているか絶対チェック</font></span>


[これを参考にして画面遷移を実装](https://reffect.co.jp/react/react-navigation)

上を参考に以下のコードを作ったがエラーが発生

```javascript
import { View, Text, Button } from 'react-native';

const HomeScreen = ({ navigation }) => {
  return (
    <View>
      <Text>ホーム画面</Text>
      <Button title="詳細" onPress={() => navigation.navigate('Details')} />
    </View>
  );
};

export default HomeScreen;
```
エラー文
```
Binding element 'navigation' implicitly has an 'any' type.ts(7031)
```
{ navigation }: { navigation: any }これで一時的にエラーを回避した


# 簡単な調査
- トランプは画像を用意する必要がある
- トランプのデータを用意して、その中に画像を値として置いておくべきかな


# 今後調べる必要があるもの
- 画像をデータとして保持して表示できるか
- キングスの役割の部分をカスタムできるようにするにはどうしたらいいか
- 